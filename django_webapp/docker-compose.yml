services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fulbacho
      POSTGRES_USER: fulbacho
      POSTGRES_PASSWORD: fulbacho
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
